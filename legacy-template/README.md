# Legacy Template Archive

This directory keeps the original Xcode template files that used to live inside
`managment-company/managment-company/`.

They were moved here so the wrapper target no longer builds unused `ContentView`,
Core Data scaffold code, or the sample model, while preserving the files for the
planned nested-repository migration.
